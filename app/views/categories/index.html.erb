<% @categories.each do |category| %>
        <ul class="listing">
            <div class="row">
                <div class= "well col-md-4 col-md-offset-4">
                    <li>
                        <% if user_signed_in? and current_user.role_id < 5 %>
                            <%= link_to "#{category.name}", category_path(category) %>
                        <% else %>
                            <%= link_to "#{category.name}", category_path(category) %>
                        <% end %>
                        </li>
                </div>
            </div>
        </ul>
    <% end %>